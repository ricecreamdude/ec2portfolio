'use strict';

angular.module('ec2portfolioApp', [
  'ec2portfolioApp.auth',
  'ec2portfolioApp.admin',
  'ec2portfolioApp.constants',
  'ngCookies',
  'ngResource',
  'ngSanitize',
  'ngRoute',
  'btford.socket-io',
  'ui.bootstrap',
  'validation.match'
])
  .config(function($routeProvider, $locationProvider) {
    $routeProvider
      .otherwise({
        redirectTo: '/'
      });

    $locationProvider.html5Mode(true);
  });
